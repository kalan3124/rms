import React from 'react';
import { APP_URL, APP_NAME } from '../../constants/config';

const LoadingPage = () => (
        <div>
                {/* <img id="spinner" src={APP_URL + 'images/logo.jpg'} /> */}
        </div>
)

export default LoadingPage;